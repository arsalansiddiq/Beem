<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arsalansiddiq.beem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- <uses-permission android:name="android.hardware.gps"/> -->

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:name=".databases.RealmApplicationInstance"
        android:allowBackup="true"
        android:icon="@mipmap/beemlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/beemlogo"
        android:supportsRtl="true">
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar1"></activity>
        <!-- <activity android:name=".activities.LoginActivity"></activity> -->

        <activity
            android:name=".activities.NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.SalesActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />

        <!-- <activity -->
        <!-- android:theme="@style/AppTheme" -->
        <!-- android:name=".MainActivity" -->
        <!-- android:windowSoftInputMode="adjustPan"/> -->

        <activity
            android:name=".activities.OrderActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan" />
        <!-- <activity -->
        <!-- android:name=".BasicActibity" -->
        <!-- android:label="@string/title_activity_basic_actibity" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->
        <activity
            android:name=".activities.TargetsAndAchievementsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar2" />
        <activity
            android:name=".activities.Replica"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar1" />
        <activity
            android:name=".activities.NotesActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.AddShopsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />

        <!-- <service android:name=".services.BreakService" -->
        <!-- android:enabled="true"/> -->

        <service
            android:name=".services.MyService"
            android:enabled="true" />
        <service android:name=".services.BreakService" />
        <service
            android:name=".services.TrackingService"
            android:enabled="true" />

        <activity
            android:name=".activities.MerchantActivity"
            android:theme="@style/AppTheme.NoActionBar1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SurveyFormQuestion"
            android:theme="@style/AppTheme.NoActionBar1"></activity>
    </application>

</manifest>